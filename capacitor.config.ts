import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firstaiduser.app',
  appName: 'Rhu-App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
