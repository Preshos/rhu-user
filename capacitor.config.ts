import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firstaiduser.app',
  appName: 'First-aid-rhu-user',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
